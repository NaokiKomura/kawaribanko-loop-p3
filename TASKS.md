# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [ ] 正本とは別のバージョン付き `localStorage` スキーマを定め、端末内投稿の読込・保存層を実装する。不正 JSON や必須フィールド欠落はそのレコードだけ隔離し、正本の表示を巻き込まない。
- [ ] 気分・タイトル・本文・返信先を入力し、プレビューで確認してから一通を保存できる投稿 UI を実装する。必須入力と文字数を検証し、保存後およびリロード後に正本の時系列へ併合表示されるところまで通す。
- [ ] 端末内投稿には「この端末のみ」と明示し、確認付き削除を提供する。正本エントリには削除操作を一切出さず、同じ見た目だけで両者を混同させない。
- [ ] 書き手フィルターの再描画でキーボードフォーカスを失わないよう修正し、絞り込みを連続操作できることを確認する。
