# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [x] 平坦な版ボタンを、親子の深さ・兄弟分岐・選択中版・各辺の版間差分・返信先変更を一画面で辿れる因果ビューへ置き換える。配列順や取込順ではなく親改訂と安定キーから表示モデルを作る純粋関数を用意し、複数階層、兄弟分岐、逆順取込でも同じ順序になることをNodeで検証する。版を切り替えたときは返信元と返事の変化も同じ文脈に表示する。あわせて編集モードを画面に明示して解除可能にし、親と新しい版の旧値／新値を比較し、無変更の子改訂はプレビューと保存の共通判定で拒否する。
- [ ] `localStorage`レコードへ世代番号または同等の比較可能なトークンを持たせ、全書込経路でread-before-writeを行う楽観的排他を実装する。古いタブは新しい改訂グラフを上書きせず、競合を画面で通知して書出し・再読込・取込の回復経路を選べるようにする。v1/v2/v3移行、通常保存、版選択、編集、取込、削除、書込例外をNodeで再現する。
- [ ] 取込だけの一回スナップショットを、取込・版選択・編集・削除を記録するappend-only操作ログへ移行し、複数段のundo/redoを再読込後も辿れるようにする。操作IDと前提世代を検証し、途中の競合または書込失敗ではログとグラフを同時に変えないこと、旧履歴を移行できることをNodeで証明する。
- [ ] 書出しに来歴情報と検証結果を添え、ハッシュが署名ではなく作成者の真正性を保証しないことを画面に明示する。欠損・内容改変・未知の来歴・正常な競合を区別して利用者が取込または回復を判断できる表示モデルを作り、悪意ある書出しを「安全」と誤表示しない回帰を追加する。
