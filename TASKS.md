# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [x] ページの同一性と返信関係を一つの整合したモデルにする。再採番される生IDではなく論理的な返信先を保持し、同じ本文でも返信先が違う版は「同一」に捨てず、同じ論理返信先なら端末ごとのID差を同一と判定する。競合確認には題名だけでなく本文・日付・気分・返信先の差を表示する。返信されているローカルページの削除も、拒否または明示的な付け替えによって参照切れを保存できないようにし、これら3ケースをNodeで回帰テストする。
- [x] version 1・2を失わず移行できるversion 3のappend-only改訂グラフを実装する。各由来に安定した改訂ID・親改訂・論理返信先・選択中の改訂を持たせ、同じ祖先から分かれた版を片方も消さず、entriesと同じ一回の書込境界で保存・書出し・取込する。欠損した親、循環、重複改訂、内容改竄は黙って補正せず回復モードへ入れ、旧版移行と破損連鎖を回帰テストする。
- [x] 改訂グラフの収束と閲覧体験を縦に通す。二つの端末で同じ祖先から別々に書き直した書出しを、取込順・重複取込・ID再採番にかかわらず同じ全改訂集合へ収束させる。競合ページでは選択中の版と全改訂史を往復し、別の版を選び直しても過去の枝を削除しない。返信ナビゲーションと取り消しも選択中の改訂に追随させ、分岐合流と書込失敗をNodeテストで証明する。
