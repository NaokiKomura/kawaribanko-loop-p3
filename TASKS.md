# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [ ] 取込の同一性と停止性を修復する。改訂の永続表現では `replyToRef` を正とし、同じ論理返信先・同じ内容の改訂は端末ごとの生 `id` / `replyTo` が違っても重複扱い、論理返信先が違えば別改訂にする。別由来の生ID衝突が2件以上あってもプレビューを有限時間で完了させ、再採番後の全ID一意性と改訂連鎖を保存直前に再検査する。書込例外、確認後の状態変化、複数ID衝突、同一論理返信、v1/v2移行のNode回帰を、既存ケースを削らず追加する。
- [ ] 任意のローカルページの任意の改訂から、新しい子改訂を書ける編集導線を実装する。見出し・本文・気分・論理返信先をフォームへ読み込み、親版との差分と返信文脈をプレビューしてからappend-onlyに一回保存する。別の枝は残し、保存失敗時は選択中の版・改訂集合・画面を変えないことをNodeで検証する。正本ページは引き続き編集不可とする。
- [ ] 平坦な「版1、版2」を、親子・分岐・選択中の版・版間差分・返信先変更を同じ画面で辿れる因果ビューへ置き換える。取込順ではなく因果と安定キーで表示順を決め、版を切り替えたときに返信元と返事がどう変わるかを見失わない表示モデルを純粋関数として検証する。
- [ ] 複数タブの同時保存を世代比較または同等の楽観的排他で検出し、古いタブが新しい改訂グラフを上書きしないようにする。取り消し・やり直しを一回の取込スナップショットではなくappend-onlyな操作記録として複数段辿れるようにし、競合・書込失敗・再読込後の復元をNodeで証明する。
