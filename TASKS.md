# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [x] 取込計画のID・返信・確認内容を保存直前まで一貫させる。衝突用IDは既存集合と取込バッチ全IDを予約して生成し、再採番後のID一意性と全 `replyTo` の解決を最終検証する。プレビュー後にローカル集合が変わった場合や、続けて選んだファイルの読取・検証が失敗した場合は古い計画を破棄する。既存ローカル保存の重複ID・参照切れも回復モードへ入れ、生成IDとバッチIDの衝突、確認後の返信先削除、連続ファイル選択、書込失敗を Node テストで再現する。
- [x] 一度きりの追加取込を、同じ書出しを何度取り込んでも増えないversion 2の冪等マージへ進める。端末や書出しに依存しない安定した由来IDと正規化した内容ハッシュを持たせ、version 1の保存・書出しを失わず移行する。「新規」「同一なので何もしない」「同じ由来で内容違い」をプレビューで分類し、内容違いは両方を見せて利用者が選べるようにする。正本は読取専用のまま保ち、再取込・順序違い・返信グラフを Node テストで証明する。
- [x] マージを可逆にするため、直前の取込について変更前スナップショットまたは同等の履歴を保存し、画面から一回取り消せるようにする。取り消しは追加・競合選択・返信の付け替えをまとめて元へ戻し、復元書込が失敗した場合は現在の保存と画面を変えない。履歴自体のversionと検証境界を定め、通常復元・破損履歴・書込失敗の回帰テストを追加する。
