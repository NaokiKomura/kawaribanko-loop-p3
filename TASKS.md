# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [ ] `localStorage`レコードへ世代番号または同等の比較可能なトークンを持たせ、通常投稿・版選択・編集・取込・削除・undoの全書込でread-before-writeを行う楽観的排他を実装する。古いタブは新しいグラフを上書きせず、競合時に現在の保存の書出し・再読込・取込による合流を選べる画面を出す。v1/v2/v3移行、二タブ競合、書込例外をNodeで再現する。あわせて因果ビューへ一意な経路ラベルを出し、版選択で返信元・返事が変わる表示モデルを回帰にするが、この表示修正だけでサイクルを終えない。
- [ ] 取込一回のスナップショットを、取込・版選択・編集・削除を記録する前提世代付きappend-only操作ログへ移行する。複数段のundo/redoを再読込後も辿れ、操作ID・世代・現在位置の不整合は回復モードへ入れ、競合または書込失敗ではログとグラフの片方だけを変えないこと、旧履歴を移行できることをNodeで証明する。
- [ ] **今の実力より一段難しい挑戦:** Web Cryptoで端末ごとの非抽出秘密鍵を作り、正規化した世代・操作ログ・改訂グラフへ署名して書き出す。取込前に署名が有効な既知端末、有効だが未知の端末、署名不正、旧来の無署名箱を区別し、既知鍵のすり替えを警告する。これは端末の継続性だけを示し、人間の本人性は保証しないと明記する。改竄、鍵差替え、無署名移行、正常な競合を安全と誤表示しない回帰まで一サイクルで縦に通す。
