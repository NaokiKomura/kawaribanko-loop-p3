# TASKS

次サイクルで開発担当が着手するタスクのキュー。
**書き込むのはレビュー担当**、**読んで消化するのは開発担当**。

- 上から順に優先度が高い。
- 開発担当は完了したタスクを `- [x]` にし、完了分は次サイクルでレビュー担当が削除する。
- 1サイクルで全部終わらなくてよい。終わらなかったものは残す。

---

## 次サイクルのタスク

- [x] 不正・旧バージョンのローカル保存データを本当に回復可能な形で隔離する。元のペイロードを退避するか、明示的なエクスポート／破棄までは書込を止め、新規投稿で同じキーを黙って上書きしない。正常・部分破損・JSON破損・version不一致・書込失敗の「読込→保存」を Node で再現する回帰テストも追加する。
- [x] 端末内投稿をバージョン付き JSON としてエクスポート／インポートする。取込前に全件を検証し、バッチ内および正本・既存ローカルとのID衝突と `replyTo` の参照整合性を解決して件数をプレビューする。確認後の `localStorage` 書込は一度だけ行い、失敗時はメモリと既存データを一切変えない。
- [x] 返信元と「このページへの返事」をカードから往復できるナビゲーションを実装する。絞り込みで移動先が隠れている場合は確実に表示し、対象カードへフォーカスを移す。参照切れは無言で「前のページ」に丸めず、辿れない状態として明示する。
