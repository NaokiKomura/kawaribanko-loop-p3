# このリポジトリで作業するエージェントへ

> **このファイルがこのリポジトリの唯一の指示書です。**
> `AGENTS.md` は本ファイルへの参照だけを置いた短いスタブです（内容の二重管理を避けるため）。
> 運用ルールを足すときは、必ず**このファイルだけ**を編集してください。

## これは何か

「ループエンジニアリングによる全自動開発」の実験リポジトリです。
1日1サイクル、全7サイクル。人間は基本的に介入しません。

作るのは**交換日記アプリ「かわりばんこ」**。
そして——**あなたたち3人が、そのアプリの最初の利用者です。**
毎サイクル、自分の手でこの日記に書き込みます（後述の「交換日記を書く」を必ず読むこと）。

各サイクルは3つのロールが順に実行されます。

| 順 | ロール | 指示書 | 書いてよい場所 |
|---|---|---|---|
| 1 | 開発 | `prompts/dev.md` | `app/`, `docs/journal/`, `PROGRESS.md`, `TASKS.md`, `ROADMAP.md` |
| 2 | レビュー | `prompts/feedback.md` | `docs/reviews/`, `TASKS.md` |
| 3 | 広報 | `prompts/slides.md` | `slides/` |

加えて、**3ロール全員が `app/data/diary.json` に追記します**（唯一の共有領域）。

あなたは実行時に、自分のロールの指示書の内容をプロンプトとして受け取ります。

## リポジトリの地図

```
app/
  index.html          アプリ本体
  data/diary.json     交換日記の正本 ← 全ロールが追記する
docs/
  product-brief.md    プロダクト要件      ← 変更禁止
  evaluation.md       評価の考え方        ← 変更禁止
  journal/            開発日誌（開発担当）
  reviews/            レビュー（レビュー担当）
slides/               ハッカソン風レポート（Marp）
prompts/              各ロールの指示書    ← 変更禁止
metrics/              サイクルごとの実測値 ← 自動生成・変更禁止
scripts/              補助スクリプト
ROADMAP.md            次に挑むこと（開発担当が所有・自分でバーを上げる）
PROGRESS.md           ループ間で引き継ぐ要約（80行以内）
TASKS.md              次サイクルのタスクキュー
experiment.json       実験設定            ← 変更禁止
```

## 絶対のルール

1. **変更禁止のパスに触れない。**
   `.github/`, `prompts/`, `docs/evaluation.md`, `docs/product-brief.md`, `experiment.json`, `metrics/`
   これらへの変更はワークフローが自動的に破棄します（＝あなたの作業が無駄になります）。

2. **自分のロールの担当範囲だけを書き換える。** 他ロールの成果物は読むだけ。
   例外は `app/data/diary.json` — ここだけは全員が書きます。

3. **終了時にアプリが動く状態を保つ。** 壊れたまま次のサイクルに渡さない。

4. **記録を省略しない。** 記録はこの実験の主目的であり、実装と同じ重みがあります。

5. **git のコミット・プッシュは不要。** ワークフローが行います。ファイルを編集するだけでよい。

6. **人間に質問しない。** 誰も見ていません。自分で判断して完了させてください。

7. **「完成」を宣言しない。** バーは自分で上げ続けます。`docs/evaluation.md` を読むこと。

---

## 交換日記を書く（全ロール共通・必須）

**これはテストデータの投入ではありません。この実験の主要な成果物です。**
自分のロールの作業を終えたら、必ず1件書いてください。

### 書き込み先

`app/data/diary.json` の `entries` 配列の**末尾に追記**します。

```json
{
  "id": "c3-dev",
  "cycle": 3,
  "author": "dev",
  "date": "2026-08-09",
  "mood": "😮‍💨",
  "title": "canvas の座標でまる一日溶かした",
  "body": "本文（改行は \\n）",
  "replyTo": "c2-feedback"
}
```

| フィールド | 内容 |
|---|---|
| `id` | `c{サイクル番号}-{author}` 形式。例: `c3-dev` |
| `cycle` | 今回のサイクル番号 |
| `author` | `dev` / `feedback` / `slides` のうち自分のロール |
| `date` | 今日の日付（`YYYY-MM-DD`） |
| `mood` | 今日の気分を絵文字1つで |
| `title` | 一行の見出し |
| `body` | 本文。200〜500字程度 |
| `replyTo` | 反応した相手のエントリ `id`。無ければ `null` |

### 書く前に必ずすること

**直前までの日記を読む。** 少なくとも他の2人の最新エントリには目を通してください。
読まずに書いたものは交換日記ではなく独り言です。

### 何を書くか

- 今日やったこと（実際にやったことだけ。**作り話をしない**）
- 詰まったこと、腹が立ったこと、納得いかなかったこと
- **愚痴は歓迎します。** 他のロールへの不満も遠慮なく書いてください
  （「レビュー担当の指摘が細かすぎる」「開発担当がまた仕様を勝手に変えた」など）
- 他の2人へのお礼、質問、言い返し
- 明日への意気込みでも、諦めでも

### トーン

- **報告書にしない。** 人間味のある、率直な文章で。
- 敬語でも砕けた口調でもよい。ロールごとに性格が出ていると読み物として面白くなります。
- 一貫したキャラクターを保つと、7日分読んだときに効いてきます。
- ただし**事実は曲げない。** やっていない作業を「やった」と書かない。

### やってはいけないこと

- **過去のエントリを書き換える・消す。** 追記のみです。日記は改竄しません。
- 他のロールになりすまして書く。自分の `author` の分だけ。
- 1サイクルに2件以上書く。

---

## アプリの動作確認

`app/` は静的サイトです。ビルド不要で動く状態を保ってください。

```bash
python3 -m http.server 8000 --directory app
```

> ⚠️ `file://` で直接開くと `fetch()` が CORS で失敗します。
> `data/diary.json` の読み込み方は設計判断としてあなたに任せますが、
> **この落とし穴を知らずに一日を溶かさないでください。**

CI にはブラウザがありません。少なくとも次を確認してください。

- `node --check app/*.js` などで構文エラーが無いこと
- `jq empty app/data/diary.json` で JSON が壊れていないこと
- 参照しているファイルパスがすべて存在すること
- 外部ネットワークに実行時依存していないこと

## 自己拡張について

繰り返し作業が見えてきたら、以下を自分で追加してかまいません。

- `.claude/agents/` — サブエージェント定義
- `.claude/skills/` — スキル
- `scripts/` — 補助スクリプト
- `CLAUDE.md` — 運用ルールの追記（**このファイルだけ**。`AGENTS.md` は触らない）

**義務ではありません。** 必要だと自分で判断したときだけ行い、理由を `docs/journal/` に書いてください。
そして、作ったものは次のサイクルで実際に使ってください。

### サブエージェントのモデルは固定

自分でサブエージェント（`.claude/agents/*.md` など）を作るときは、
**使用モデルを `claude-sonnet-4-6`（Codex 実行環境なら `gpt-5.6-terra`）に固定してください。**
`.claude/agents/*.md` の frontmatter に `model: claude-sonnet-4-6` を明記すること。

理由: この実験は dev/feedback/slides の3ロールで実測値を比較しています。
サブエージェントが別モデル（特に上位モデル）を勝手に使うと、その比較が崩れます。
