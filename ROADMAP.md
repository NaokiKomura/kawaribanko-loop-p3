# ROADMAP

**開発エージェントが所有するファイル**です。ここに「次に挑むこと」を自分で書きます。

固定の到達段階（M1、M2…）はこの実験には存在しません。バーは自分で決め、**自分で上げます**。
考え方は `docs/evaluation.md` を読むこと。

## ルール

1. 毎サイクル、必ず更新する。
2. **目標を達成したら、次はより難しい目標に更新する。** 楽なタスクに逃げない。
3. 「次に挑むこと」を**空にしてはいけない**。最終サイクルまで完成を宣言しない。
4. 「まだ満足していない点」を毎回1つ以上書く。ここが次のバーの種になる。
5. フィードバックエージェントは、バーが下がっていれば `LOWERED` を出して指摘してくる。

---

## 今の到達点

- JSON を正本として読み込み、日付・サイクル・IDで安定して時系列表示する閲覧画面がある。
- `members` の名前・絵文字・色をカードと絞り込みに反映し、各書き手のページを選べる。
- 読み込み失敗と、まだ書かれていない人のページを画面内で案内する。
- 正本と分離したバージョン付き `localStorage` に、この端末だけの一通を保存できる。必須項目・文字数を検証し、プレビュー後に保存、時系列への併合、確認付き削除まで行える。
- ローカル投稿だけに「この端末のみ」の札と削除操作を出し、壊れた正本／ローカル記録は該当分を隔離して他のページを読み続けられる。
- 読めないローカル保存は元の文字列を残して投稿・取込を停止し、書き出しまたは明示的な破棄まで上書きしない。
- ローカル投稿をversion付きJSONで引っ越せる。全件検証、返信先検証、ID衝突時の再採番、確認後一回だけの保存を行い、失敗時は既存記録を変えない。
- 各ページから返信元と返事のページへ往復でき、絞り込みで隠れた移動先も表示してフォーカスする。
- version 3では、由来ごとの親改訂・論理返信先・選択中の改訂をappend-onlyなグラフで保存する。同じ祖先から分岐した版も失わず、取込順・重複取込・端末ごとの生ID差に依存せずに全改訂集合へ合流する。競合の差と改訂史をカードで読み、表示版を選び直せる。version 1・2と旧v2の一回取り消し履歴も読み取り時に移行する。

## 次に挑むこと

- [ ] 改訂を「読む」帳面から、自分で因果を保ったまま「書き直せる」共同日記へ進める
      — カード上の任意の改訂を起点に、利用者が新しい子改訂を下書き・比較・保存できる編集導線を作る。改訂史と返信の会話を一つの時間線として辿り、選択中の版が変わったときに返信の前後関係がどう変わるかを見える形にする。別タブ／別端末の同時編集を、世代比較と再取込で誤上書きせず収束させ、複数回の取り消し・やり直しもappend-onlyな操作記録として扱う。識別用ハッシュと真正性の限界を画面・書出し形式で明示し、改訂の欠損・偽造・競合を利用者が回復可能な形で判断できることをNodeテストで証明する。正本は引き続き読取専用とする。

## まだ満足していない点

- 端末間の引っ越しと分岐合流は手動の書出し・取込に限られ、機種変更時の導線や合流前の来歴の説明はまだ重い。
- 返信先は一行の注記でしかなく、会話の前後を改訂史と重ねて辿れない。日記の往復が長くなるほど文脈を見失う。
- 改訂の枝は保存・選択できるようになったが、利用者自身が既存の一通を書き直す導線がない。返信の糸と改訂の親子関係も一画面で読めず、分岐が増えるほど文脈を組み立てにくい。
- 識別ハッシュは悪意ある改竄を防ぐ署名ではなく、複数タブでの同時保存も直列化されない。長い改訂史を可逆に整理する運用もない。

## 挑戦の履歴

| サイクル | 掲げた目標 | 結果 | 次に上げたバー |
|---|---|---|---|
| 1 | JSON正本を読む最低要件と書き手の識別 | 達成。静的サーバーで読める閲覧画面を実装 | 正本を傷つけず、端末内の投稿を併合する書く体験 |
| 2 | 正本を傷つけず、端末内の投稿を併合する書く体験 | 達成。検証・プレビュー・永続化・明示的な削除境界を実装 | 取込の原子性とID衝突まで扱う端末間の引っ越し、返信関係のナビゲーション |
| 3 | 取込の原子性とID衝突まで扱う端末間の引っ越し、返信関係のナビゲーション | 達成。回復ロック、全件検証、衝突再採番、一回の保存、返信往復、Node回帰テストを実装 | 由来・重複排除・衝突選択・取り消しまで備えた可逆マージ |
| 4 | 由来・重複排除・衝突選択・取り消しまで備えた可逆マージ | 達成。version 2の由来ID／内容ハッシュ、冪等取込、競合選択、取込スナップショットの一回取り消し、version 1移行を実装 | 選ばれなかった改訂も失わない、改訂グラフによる監査可能な収束 |
| 5 | 選ばれなかった改訂も失わない、改訂グラフによる監査可能な収束 | 達成。version 3の親改訂・論理返信先・選択中版、分岐合流、改訂表示、v1/v2移行、回復境界を実装 | 因果を壊さず利用者自身が書き直せる共同編集と、長い改訂史の可逆な運用 |
