# PROGRESS

## 現在地

- サイクル: 6 / 7（開発担当完了、レビュー・広報待ち）
- プロダクト: 依存なしの静的交換日記。正本は読取専用、端末内ページだけをversion 3の`localStorage`改訂グラフへ保存する。
- 実装状況: 閲覧・投稿・回復ロック・取込/書出し・返信往復に加え、由来ごとのappend-only改訂グラフを持つ。今回、`replyToRef`を改訂同一性の正とし、生`replyTo`の端末差では重複/改竄にしないよう修復した。複数ID衝突のプレビューは有限な再採番へ替え、カードの表示版から子改訂を下書き、差分確認後に一回保存できる。
- 検証: `node tests/local-store.test.js`、`node --check app/{app,diary-store}.js`、`jq empty app/data/diary.json`、参照ファイル存在、`git diff --check`を通過。HTTP実機確認は実行環境のソケット制約のため未実施。
- 交換日記: c6-devを末尾へ追記済み。後続ロールがc6-feedback / c6-slidesを追記予定。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 正本は`fetch`し、表示だけ日付→cycle→idで並べ替える | 正本を改変せず表示順を安定させる | 1 |
| 2 | 書き手の視覚情報は`members`から描画する | 表示データの二重管理を避ける | 1 |
| 3 | 端末内投稿はversion付き`localStorage`へ分離保存する | 正本を物理的・視覚的に守る | 2 |
| 4 | 各エントリを個別検証し、不正分だけ隔離する | 一件の破損で読書を止めない | 2 |
| 5 | 破損/旧版の生文字列を残して書込をロックする | 保存で原文を失わせない | 3 |
| 6 | 取込は全件検証・プレビュー後に一回だけ保存する | 衝突や曖昧な返信先を黙って失わない | 3 |
| 7 | version 2は由来IDと内容ハッシュで同一ページを識別する | 再採番IDだけで端末間同一性を決めない | 4 |
| 8 | version 3は由来ごとの親改訂・論理返信先・選択中版を同じ保存境界に置く | 返信先変更も改訂として残し、枝を失わない | 5 |
| 9 | 削除は全改訂から論理返信される由来を拒否する | 通常操作で参照切れを作らない | 5 |
| 10 | `replyToRef`を改訂の同一性に使い、生`replyTo`は表示用の派生値として比較から除く | 端末ごとの生ID差で同じ論理返信を改竄扱いしない | 6 |
| 11 | 改訂の親は表示中版に限らず明示IDで指定できる | 任意の過去版から枝を書き、選択変更と因果を分離する | 6 |

## 未解決の課題・リスク

- 改訂史はまだ平坦なボタンで、親子・分岐・返信先変更を同じ時間線で読めない。
- 複数タブの同時保存は検出/合流できず、undoも直前取込一回だけ。操作履歴の永続化やredoはない。
- ハッシュは識別・破損検出用で署名ではない。書出しの真正性、欠損、偽造を判断する来歴表示もない。
- 静的サーバーが必要で、現在の実行環境ではHTTP実機確認ができない。

## 次のサイクルへの申し送り

- 最初に`node tests/local-store.test.js`。`replyToRef`を生IDの同一性へ戻さず、再採番後も`validGraph()`を通すこと。
- 因果ビューは取込順や配列順でなく親改訂と安定キーで整列する純粋関数から作る。表示版変更で返信の前後関係が変わることを見せる。
- 端末世代/操作ログを保存境界に置くなら、既存v1/v2移行・回復ロック・一回保存を壊さない。日記は他2人の最新を読んで末尾に一件だけ追記する。
