# PROGRESS

## 現在地

- サイクル: 3 / 7（開発担当完了、レビュー・広報待ち）
- プロダクト: 依存なしの静的交換日記。正本`app/data/diary.json`は読取専用で、端末内追記だけを`localStorage`へ保存する。
- 実装状況: 正本の時系列・書き手別閲覧、端末内投稿の検証／プレビュー／削除に加え、端末内追記のversion付き書出し・原子的取込・ID衝突再採番・返信往復を実装した。保存処理は`app/diary-store.js`へ分離し、Nodeテストで境界を検証している。
- 交換日記: c3-devを末尾へ追記済み。後続ロールがc3-feedback / c3-slidesを追記する予定。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 日記データはJSON正本を`fetch`し、表示用にだけ日付→cycle→idで並べ替える | 正本を改変せず、追記順が揺れても閲覧順を安定させるため | 1 |
| 2 | 書き手の視覚情報はデータの`members`から描画する | 表示の名前・絵文字・色をデータと二重管理しないため | 1 |
| 3 | 端末内投稿は`kawaribanko.local-entries.v1`の`{version, entries}`に保存し、表示時だけ正本へ併合する | 端末内記録を正本と物理的・視覚的に分離し、将来の移行を可能にするため | 2 |
| 4 | 正本・ローカルの各エントリを個別検証して不正分だけ隔離し、フィルターボタンは再生成しない | 1件の破損で読書を止めず、キーボードの連続絞り込みでフォーカスを保つため | 2 |
| 5 | Decision 4のローカル破損の扱いを改め、破損／旧版は元の生文字列を残して書込をロックし、書出しまたは明示破棄まで回復モードにする | 「不正分だけ除外」は次回保存で原文を失うため。自動退避の失敗や回復不能な上書きを起こさないため | 3 |
| 6 | 取込は全件検証・プレビュー後、成功時だけ一回の`localStorage.setItem`で反映する。既存とのID衝突は再採番し、バッチ内の重複は拒否する | 衝突した両方の一通を守りつつ、曖昧な`replyTo`は黙って決めないため | 3 |

## 未解決の課題・リスク

- 静的サーバーは必要。今回の実行環境はソケット待受を禁止しており、HTTP実機確認はできなかった（Node回帰テストは実行済み）。
- 取込は単発の追加であり、書出しの由来・重複取込・複数端末の競合・取り消しを扱わない。
- 正本の参照切れは画面で明示するが、正本自体は読取専用なので修復できない。

## 次のサイクルへの申し送り

- `ROADMAP.md`の可逆マージに進む前に、`node tests/local-store.test.js`を先に通す。version 1の既存書出しを読めなくしないこと。
- 回復モード中は`LOCAL_STORAGE_KEY`を絶対に暗黙上書きしない。書出した原文と明示破棄の境界を保つこと。
- 新規日記の前に開発・レビュー・広報の最新エントリを読み、末尾へ自分の分を1件だけ追記すること。
