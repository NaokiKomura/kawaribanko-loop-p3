# PROGRESS

## 現在地

- サイクル: 7 / 7（開発担当完了、レビュー・広報待ち）
- プロダクト: 依存なしの静的交換日記。正本は読取専用、端末内ページだけをversion 3の`localStorage`改訂グラフへ保存する。
- 実装状況: 閲覧・投稿・回復ロック・取込/書出し・返信往復・由来ごとのappend-only改訂グラフがある。改訂帳は親IDと改訂IDの安定順から因果ビューを作り、枝・深さ・選択版・各辺の差分・返信先を示す。編集モードは解除でき、プレビューと保存の両方が無変更の子を拒否する。
- 検証: `node tests/local-store.test.js`、`node --check app/{app,diary-store}.js`、`jq empty app/data/diary.json`、参照ファイル存在、`git diff --check`を通過。HTTP実機確認は実行環境のソケット制約で起動拒否された。
- 交換日記: c7-devを末尾へ追記済み。後続ロールがc7-feedback / c7-slidesを追記予定。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 正本は`fetch`し、表示だけ日付→cycle→idで並べ替える | 正本を改変せず表示順を安定させる | 1 |
| 2 | 書き手の視覚情報は`members`から描画する | 表示データの二重管理を避ける | 1 |
| 3 | 端末内投稿はversion付き`localStorage`へ分離保存する | 正本を物理的・視覚的に守る | 2 |
| 4 | 各エントリを個別検証し、不正分だけ隔離する | 一件の破損で読書を止めない | 2 |
| 5 | 破損/旧版の生文字列を残して書込をロックする | 保存で原文を失わせない | 3 |
| 6 | 取込は全件検証・プレビュー後に一回だけ保存する | 衝突や曖昧な返信先を黙って失わない | 3 |
| 7 | version 2は由来IDと内容ハッシュで同一ページを識別する | 再採番IDだけで端末間同一性を決めない | 4 |
| 8 | version 3は由来ごとの親改訂・論理返信先・選択中版を同じ保存境界に置く | 返信先変更も改訂として残し、枝を失わない | 5 |
| 9 | 削除は全改訂から論理返信される由来を拒否する | 通常操作で参照切れを作らない | 5 |
| 10 | `replyToRef`を改訂の同一性に使い、生`replyTo`は表示用の派生値として比較から除く | 端末ごとの生ID差で同じ論理返信を改竄扱いしない | 6 |
| 11 | 改訂の親は表示中版に限らず明示IDで指定できる | 任意の過去版から枝を書き、選択変更と因果を分離する | 6 |
| 12 | 因果ビューは親改訂IDと決定的な改訂ID順から純粋に導き、表示順を保存配列へ預けない | 逆順取込でも親子・兄弟・差分を同じように読む | 7 |
| 13 | 無変更判定は版間差分を唯一の規則としてプレビューと保存で共有する | 空の子改訂を画面表示と保存のどちらからも作らせない | 7 |

## 未解決の課題・リスク

- `localStorage`に世代比較がなく、複数タブは古いグラフで新しい保存を上書きできる。
- 操作履歴は直前取込一回のスナップショットだけで、編集・選択・削除の永続undo/redoがない。
- ハッシュは署名でない。書出しの来歴、欠損、偽造、競合を安全に判断する表示がない。
- 静的サーバーが必要で、現在の実行環境ではHTTP実機確認を起動できない。

## 次のサイクルへの申し送り

- 最初に`node tests/local-store.test.js`。`causalView()`を取込配列順に戻さず、`replyToRef`を生ID比較へ戻さないこと。
- version 4へ進めるならv1/v2/v3移行・回復ロック・一回保存を残し、read-before-write世代比較を全書込に通すこと。
- 操作ログはグラフと同じ保存境界で原子的に扱い、競合/例外時に片方だけ変わらないNode回帰を先に書く。日記は他2人の最新を読んで末尾へ一件だけ追記する。
