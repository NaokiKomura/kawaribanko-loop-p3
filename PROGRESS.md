# PROGRESS

## 現在地

- サイクル: 5 / 7（開発担当完了、レビュー・広報待ち）
- プロダクト: 依存なしの静的交換日記。`app/data/diary.json`は読取専用で、端末内追記だけを`localStorage`へ保存する。
- 実装状況: 閲覧・端末内投稿・回復ロック・返信往復に加え、version 3 のappend-only改訂グラフで端末間取込を合流する。ページ由来ごとに親改訂・論理返信先・選択中の版を保存し、分岐は捨てずにカードで表示版を選べる。保存処理は`app/diary-store.js`、回帰は`tests/local-store.test.js`に集約した。
- 交換日記: c5-devを末尾へ追記済み。後続ロールがc5-feedback / c5-slidesを追記する予定。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 日記データはJSON正本を`fetch`し、表示用にだけ日付→cycle→idで並べ替える | 正本を改変せず、追記順が揺れても閲覧順を安定させるため | 1 |
| 2 | 書き手の視覚情報はデータの`members`から描画する | 表示の名前・絵文字・色をデータと二重管理しないため | 1 |
| 3 | 端末内投稿はversion付き`localStorage`に保存し、表示時だけ正本へ併合する | 端末内記録を正本と物理的・視覚的に分離するため | 2 |
| 4 | 正本・ローカルの各エントリを個別検証して不正分だけ隔離し、フィルターボタンは再生成しない | 1件の破損で読書を止めず、連続絞り込みのフォーカスを保つため | 2 |
| 5 | 破損／旧版は元の生文字列を残して書込をロックし、書出しまたは明示破棄まで回復モードにする | 次回保存による原文喪失を防ぐため | 3 |
| 6 | 取込は全件検証・プレビュー後、成功時だけ一回の`setItem`で反映する | 衝突ページと曖昧な返信先を黙って失わないため | 3 |
| 7 | version 2では由来IDと内容ハッシュで同一ページを識別し、直前取込だけを一回取り消す | 再採番IDだけでは端末間の同一性を決められないため | 4 |
| 8 | version 3は由来ごとのappend-only改訂グラフ、親改訂、論理返信先、選択中改訂を本文と同じ保存境界に置く | 返信先変更も改訂として残し、分岐・取込順・端末ID差で版を失わないため | 5 |
| 9 | 削除は他ページから論理返信されている由来を拒否し、選択変更は改訂集合を消さない | 通常操作で参照切れや選ばれなかった枝の喪失を作らないため | 5 |

## 未解決の課題・リスク

- 静的サーバーは必要。実行環境はソケット待受を禁止し、HTTP実機確認はできなかった（Node回帰は実行済み）。
- 取込済みの分岐は読んで選べるが、利用者が既存ページから新しい改訂を作る編集UIはまだない。改訂と返信の会話文脈も別々に辿る。
- 改訂IDと内容ハッシュは識別・破損検出用で、暗号学的な署名ではない。悪意ある書出しの真正性は保証しない。
- 取り消しは直前の取込一回だけで、複数操作をまたぐ取り消し／やり直しや複数タブの同時書込みは扱わない。

## 次のサイクルへの申し送り

- `node tests/local-store.test.js`から始める。`replyToRef`は`origin:` / `source:`の論理参照であり、生IDを同一性へ戻さないこと。
- v1/v2移行と回復ロックを壊さない。version 3の改訂・履歴もentriesと同じ一回の保存境界に置くこと。
- 新規日記の前に開発・レビュー・広報の最新エントリを読み、末尾へ自分の分を1件だけ追記すること。
