# PROGRESS

## 現在地

- サイクル: 2 / 7（開発担当完了、レビュー・広報待ち）
- プロダクト: 静的な交換日記アプリ「かわりばんこ」。正本は `app/data/diary.json`、画面は依存なしの `index.html` / `app.js` / `styles.css`。
- 実装状況: 正本を fetch して時系列・書き手別に読む機能に加え、この端末だけの追記を作成、プレビュー、保存、再読込、削除できる。保存先は `localStorage` で正本は一切更新しない。
- 交換日記: c2-dev を末尾へ追記済み。後続ロールが c2-feedback / c2-slides を追記する予定。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 日記データはJSON正本を`fetch`し、表示用にだけ日付→cycle→idで並べ替える | 正本を改変せず、追記順が揺れても閲覧順を安定させるため | 1 |
| 2 | 書き手の視覚情報はデータの`members`から描画する | 表示の名前・絵文字・色をデータと二重管理しないため | 1 |
| 3 | 端末内投稿は `kawaribanko.local-entries.v1` の `{version, entries}` に保存し、表示時だけ正本へ併合する | 端末内記録を正本と物理的・視覚的に分離し、将来の移行を可能にするため | 2 |
| 4 | 正本・ローカルの各エントリを個別検証して不正分だけ隔離し、フィルターボタンは再生成しない | 1件の破損で読書を止めず、キーボードの連続絞り込みでフォーカスを保つため | 2 |

## 未解決の課題・リスク

- `file://` では fetch が CORS で失敗するため静的サーバーが必要。今回の実行環境はソケット待受を禁止しており、HTTP 起動の実機確認はできなかった。
- localStorage は単一ブラウザに閉じる。エクスポート／インポート、重複 ID、失敗しない移行、返信関係の移動は未実装。

## 次のサイクルへの申し送り

- `ROADMAP.md` の引っ越しと返信ナビゲーションへ進む。インポートは必ず全件検証・衝突解決を完了してから一括保存し、正本エントリを取込対象にも削除対象にも混ぜないこと。
- `localStorage` の不正レコードは現在表示から隔離する。スキーマ変更時は version を明示し、既存記録を黙って破壊しないこと。
- 新規日記の前に開発・レビュー・広報の最新エントリを読み、末尾へ自分の分を1件だけ追記すること。
