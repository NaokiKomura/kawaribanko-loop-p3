# PROGRESS

このファイルは**ループ間で唯一引き継がれる記憶の要約**です。
各サイクルの開発担当が、これを読んでから作業を始め、作業後に書き換えます。

> ルール: 全体で **80行以内** に収めること。
> 増やすのではなく「圧縮して書き換える」。詳細は `docs/journal/` に置き、ここには残さない。

---

## 現在地

- サイクル: 1 / 7（開発担当完了、レビュー・広報待ち）
- プロダクト: 交換日記アプリ「かわりばんこ」（要件は `docs/product-brief.md`）
- 実装状況: `app/index.html` + `app.js` + `styles.css` の静的閲覧アプリ。`fetch("data/diary.json")` で正本を読み、時系列表示・書き手の色/名前/絵文字・フィルター・空状態/読み込み失敗を扱う。
- 交換日記: オーナーと開発担当のエントリがある（後続ロールが同サイクル分を追記予定）。

## アーキテクチャ上の決定（Decision Log）

| # | 決定したこと | 理由 | サイクル |
|---|---|---|---|
| 1 | 日記データはJSON正本を`fetch`し、表示用にだけ日付→cycle→idで並べ替える | 正本を改変せず、追記順が揺れても閲覧順を安定させるため | 1 |
| 2 | 書き手の視覚情報はデータの`members`から描画する | 表示の名前・絵文字・色をデータと二重管理しないため | 1 |

## 未解決の課題・リスク

- `file://` では `fetch()` がCORSで失敗するため、静的サーバーで開く必要がある（画面にも失敗時の案内あり）。
- 正本JSONを操作せずにブラウザ入力を保存・表示する、ローカル投稿のスキーマと削除方針は未設計。

## 次のサイクルへの申し送り

- 次は`ROADMAP.md`のローカル投稿機能に挑戦する。正本エントリは絶対に書換え・削除せず、端末内投稿と明確に区別すること。
- 新規日記を書く前に、開発・レビュー・広報の最新エントリを必ず読み、末尾へ1件だけ追記すること。
